//! Typed configuration surface for ChainView.
//!
//! Will define `Config`: provider/mode selection, credentials from the
//! environment, refresh intervals, and CLI > env > file > default source
//! precedence. Lands with issue #3; this stub carries no logic.
