//! CLI entry point for the `chainview` binary.
//!
//! Will own mode/provider selection, terminal setup with guaranteed restore,
//! and the tokio runtime. No runtime behavior exists yet — the skeleton
//! builds clean under the full lint set.

#![forbid(unsafe_code)]

fn main() {}
