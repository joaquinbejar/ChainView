//! Boundary error types for ChainView.
//!
//! Will define `ChainViewError` (`thiserror`) — the shared boundary every
//! provider, bundle, config, and terminal error converts into. Lands with
//! issue #2; this stub carries no logic.
