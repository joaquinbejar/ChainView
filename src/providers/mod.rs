//! The provider port and its adapters.
//!
//! Will define the PUBLIC, semver-governed provider port — the `Provider`
//! trait plus `ProviderCapabilities` — and host the built-in venue adapters
//! that normalize upstream payloads into the domain model. Lands with
//! issue #6 onward; this stub carries no logic.
