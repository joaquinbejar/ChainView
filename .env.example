# ChainView environment configuration.
#
# No secrets are ever committed to this file — it documents the variables the
# typed config surface (issue #3) will read, each with its default and range.
# Zero-config default: public Deribit data requires no credentials at all.
